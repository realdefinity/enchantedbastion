<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>

    
    :root {
      --gold: #d4af37;
      --gold-light: #f8e8b0;
      --gold-dark: #b38f2a;
      --ivory: #f8f4e9;
      --black: #121212;
      --black-light: #1a1a1a;
      --parchment: #f8f4e9; 
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    html {
      scroll-behavior: smooth;
    }
    
    body {
      font-family: 'Cormorant Garamond', serif;
      color: var(--ivory);
      background: var(--black);
      overflow-x: hidden;
      min-height: 100vh;
      line-height: 1.6;
    }
    
    /* ===== EPIC COSMIC BACKGROUND ===== */
    .hero-section {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      background: 
        radial-gradient(ellipse at bottom, #1B2735 0%, #090A0F 100%),
        url('https://images.unsplash.com/photo-1534447677768-be436bb09401?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1491&q=80') center/cover no-repeat;
    }
    
    .hero-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(58,15,15,0.3) 0%, transparent 100%);
      z-index: 1;
    }
    
    .hero-content {
      position: relative;
      z-index: 2;
      text-align: center;
      padding: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }
    
    .hero-title {
      font-family: 'Cinzel Decorative', serif;
      font-weight: 900;
      font-size: clamp(3rem, 10vw, 6rem);
      line-height: 1.1;
      margin-bottom: 1.5rem;
      color: transparent;
      background: linear-gradient(135deg, var(--gold) 0%, var(--gold-light) 50%, var(--gold-dark) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      text-shadow: 0 4px 30px rgba(0, 0, 0, 0.4);
      letter-spacing: 0.05em;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeInUp 1.5s cubic-bezier(0.22, 1, 0.36, 1) forwards 0.5s;
    }
    
    .hero-subtitle {
      font-family: 'Playfair Display', serif;
      font-size: clamp(1.3rem, 3.5vw, 2.2rem);
      color: var(--gold-light);
      letter-spacing: 0.2em;
      text-transform: uppercase;
      margin-bottom: 3rem;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeInUp 1.2s cubic-bezier(0.22, 1, 0.36, 1) forwards 0.8s;
    }
    
    .hero-cta {
      display: inline-block;
      padding: 1rem 2.5rem;
      background: transparent;
      color: var(--gold);
      border: 2px solid var(--gold);
      border-radius: 50px;
      font-family: 'Cinzel Decorative', serif;
      font-size: 1.2rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      cursor: pointer;
      transition: all 0.6s cubic-bezier(0.22, 1, 0.36, 1);
      position: relative;
      overflow: hidden;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeInUp 1s cubic-bezier(0.22, 1, 0.36, 1) forwards 1s;
      text-decoration: none;
      margin-top: 2rem; /* Added space between image & button */
      z-index: 10; /* Ensures button stays above everything */
    }
    
    .hero-cta:hover {
      background: rgba(212, 175, 55, 0.1);
      box-shadow: 0 0 30px rgba(212, 175, 55, 0.5);
      transform: translateY(-5px);
    }
    
    .hero-image-container {
      position: relative;
      width: 80%;
      max-width: 800px;
      margin: 2rem auto 0;
      perspective: 1000px;
      opacity: 0;
      transform: translateY(50px) scale(0.95);
      animation: fadeInUp 1.5s cubic-bezier(0.22, 1, 0.36, 1) forwards 1.2s;
    }
    
    .hero-image-wrapper {
      position: relative;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 40px 80px rgba(0, 0, 0, 0.8);
      transform-style: preserve-3d;
      transition: transform 0.8s cubic-bezier(0.22, 1, 0.36, 1);
      border: 1px solid rgba(212, 175, 55, 0.3);
    }
    
    .hero-image {
      width: 100%;
      height: auto;
      display: block;
      transform: scale(1.05);
      transition: transform 1s cubic-bezier(0.22, 1, 0.36, 1);
    }
    
    .image-reflection {
      position: absolute;
      bottom: -60%;
      left: 0;
      width: 100%;
      height: 60%;
      background: linear-gradient(to top, rgba(212, 175, 55, 0.1), transparent);
      transform: scaleY(-1);
      opacity: 0.4;
      filter: blur(10px);
      z-index: -1;
    }
    
  /* ===== BACKGROUND ===== */
    .starfield {
       position: fixed;
       top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
      overflow: hidden;
    }

    .star {
      position: absolute;
      background: white;
      border-radius: 50%;
      animation: twinkle var(--duration) infinite ease-in-out;
      opacity: 0;
    }

    .hero-content {
      margin-top: 80px;
    }

    .header {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      padding: 20px;
      text-align: center;
      background: rgba(0,0,0,0.7);
      z-index: 100;
      backdrop-filter: blur(5px);
    }

    .header h1 {
      font-family: 'Cinzel Decorative', serif;
      color: var(--gold);
      font-size: 1.8rem;
      margin: 0;
    }


    /* Responsive Adjustments */
    @media (max-width: 768px) {
      .staff-container {
        padding: 0 1.5rem;
      }
      .employee-meta {
        gap: 1rem;
      }
      .employee-structure {
        grid-template-columns: 1fr;
      }
      
      .employee-item {
        flex-wrap: wrap;
      }
      
      .position, .count, .salary {
        width: 100%;
        text-align: left;
      }
      .salary {
        min-width: 70px;
      }
      .count, .salary {
        margin-top: 0.3rem;
      }
    }


    /* ===== CURSOR ===== */
    html {
      cursor: none;
    }

    .cursor-ring {
      position: fixed;
      width: 30px;
      height: 30px;
      border: 2px solid var(--gold);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9999;
      mix-blend-mode: difference;
      transition: 
        transform 0.3s ease,
        width 0.3s ease,
        height 0.3s ease,
        background 0.3s ease;
      transform: translate(-50%, -50%);
    }

    .cursor-dot {
      position: fixed;
      width: 6px;
      height: 6px;
      background-color: var(--gold-light);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9998;
      transform: translate(-50%, -50%);
      transition: transform 0.1s ease;
    }

    /* Cursor hover states */
    a:hover ~ .cursor-ring,
    button:hover ~ .cursor-ring,
    .department-card:hover ~ .cursor-ring {
      transform: translate(-50%, -50%) scale(1.5);
      background: rgba(212, 175, 55, 0.2);
      border-color: var(--gold-light);
    }

    .hero-cta:hover ~ .cursor-ring {
      transform: translate(-50%, -50%) scale(2);
      background: rgba(212, 175, 55, 0.3);
    }

    html {
      scroll-behavior: smooth;
      scroll-padding-top: 80px;
    }

    .department-card {
      will-change: transform, box-shadow;
      transform-style: preserve-3d;
      backface-visibility: hidden;
    }

    .gold-particle {
      will-change: transform, opacity;
      animation: floatParticle linear infinite;
      opacity: 0;
    }

    @keyframes floatParticle {
      0%, 100% { 
        transform: translate(0, 0) rotate(0deg); 
        opacity: 0; 
      }
      20% { opacity: 0.4; }
      80% { opacity: 0.4; }
      100% { 
        transform: translate(random(200) - 100px, -100vh) rotate(360deg); 
        opacity: 0; 
      }
    }

        @keyframes twinkle {
      0%, 100% { opacity: 0; transform: scale(0.5); }
      50% { opacity: var(--opacity); transform: scale(1); }
    }

    /* ===== ANIMATIONS ===== */
    @keyframes fadeInUp {
  0% { 
    opacity: 0; 
    transform: translateY(30px) scale(0.98);
    filter: blur(2px);
  }
  100% { 
    opacity: 1; 
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}


    /* ===== STAFF STYLES ===== */
    .staff-section {
      padding: 6rem 2rem;
      position: relative;
      background: linear-gradient(to bottom, rgba(10,10,10,0.95), rgba(5,5,5,0.98));
    }

    .staff-container {
      max-width: 1200px;
      margin: 0 auto;
    }

    .section-header {
      text-align: center;
      margin-bottom: 4rem;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeInUp 1s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }

    .employee-structure {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 2rem;
      perspective: 1000px;
    }

    .department-card {
      width: 320px;
      background: linear-gradient(145deg, rgba(30,30,30,0.95), rgba(18,18,18,0.98));
      border-radius: 16px;
      padding: 2rem;
      border: 1px solid rgba(212,175,55,0.15);
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      position: relative;
      overflow: hidden;
      transform-style: preserve-3d;
      transition: all 0.6s cubic-bezier(0.22, 1, 0.36, 1);
      opacity: 0;
      transform: translateY(50px) rotateX(15deg);
      animation: cardEntrance 0.8s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }

    @keyframes cardEntrance {
      0% {
        opacity: 0;
        transform: translateY(50px) rotateX(15deg);
      }
      100% {
        opacity: 1;
        transform: translateY(0) rotateX(0);
      }
    }

    .department-card:nth-child(1) { animation-delay: 0.2s; }
    .department-card:nth-child(2) { animation-delay: 0.4s; }
    .department-card:nth-child(3) { animation-delay: 0.6s; }
    .department-card:nth-child(4) { animation-delay: 0.8s; }

    .card-glow {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at 50% 0%, rgba(212,175,55,0.1), transparent 70%);
      opacity: 0;
      transition: opacity 0.6s ease;
    }

    .department-card:hover {
      transform: translateY(-10px) scale(1.03) rotateY(5deg);
      box-shadow: 0 25px 60px rgba(0,0,0,0.5), 
                  0 0 0 1px rgba(212,175,55,0.4);
      border-color: rgba(212,175,55,0.5);
    }

    .department-card:hover .card-glow {
      opacity: 1;
    }

    .department-header {
      display: flex;
      align-items: center;
      gap: 1.5rem;
      margin-bottom: 1.5rem;
      padding-bottom: 1.5rem;
      position: relative;
    }

    .department-header::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(212,175,55,0.5), transparent);
    }

    .department-icon {
      font-size: 2.2rem;
      width: 60px;
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(212,175,55,0.1);
      border-radius: 12px;
      border: 1px solid rgba(212,175,55,0.2);
      transition: all 0.5s cubic-bezier(0.22, 1, 0.36, 1);
    }

    .department-card:hover .department-icon {
      transform: scale(1.2) rotate(10deg);
      background: rgba(212,175,55,0.2);
      box-shadow: 0 0 20px rgba(212,175,55,0.3);
    }

    .department-title {
      font-family: 'Cinzel', serif;
      color: var(--gold);
      font-size: 1.4rem;
      margin-bottom: 0.3rem;
      letter-spacing: 0.05em;
      transition: all 0.3s ease;
    }

    .department-card:hover .department-title {
      letter-spacing: 0.08em;
      text-shadow: 0 0 10px rgba(212,175,55,0.5);
    }

    .employee-count {
      font-size: 0.9rem;
      color: var(--gold-light);
      background: rgba(212,175,55,0.1);
      padding: 0.3rem 0.8rem;
      border-radius: 20px;
      display: inline-block;
      border: 1px solid rgba(212,175,55,0.2);
      transition: all 0.3s ease;
    }

    .department-card:hover .employee-count {
      background: rgba(212,175,55,0.2);
      transform: scale(1.1);
    }

    .employee-list {
      list-style-type: none;
      margin-top: 1.5rem;
    }

    .employee-item {
      display: flex;
      justify-content: space-between;
      padding: 1rem 0;
      position: relative;
      transition: all 0.4s ease;
      border-bottom: 1px dashed rgba(212,175,55,0.1);
    }

    .employee-item:last-child {
      border-bottom: none;
    }

    .employee-item:hover {
      transform: translateX(10px);
      border-bottom-color: rgba(212,175,55,0.3);
    }

    .position {
      color: var(--ivory);
      flex: 1;
      transition: all 0.3s ease;
      font-family: 'EB Garamond', serif;
      font-size: 1.1rem;
    }

    .employee-item:hover .position {
      color: var(--gold-light);
      letter-spacing: 0.5px;
    }

    .count, .salary {
      font-family: 'Cinzel', serif;
      transition: all 0.3s ease;
    }

    .count {
      color: var(--gold);
      width: 50px;
      text-align: right;
    }

    .salary {
      color: var(--gold-light);
      width: 100px;
      text-align: right;
      font-size: 0.9em;
    }

    .employee-item:hover .count,
    .employee-item:hover .salary {
      transform: scale(1.1);
      text-shadow: 0 0 8px rgba(212,175,55,0.3);
    }

    /* Floating particles effect */
    .department-card::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(212,175,55,0.05) 0%, transparent 70%);
      opacity: 0;
      transition: opacity 0.8s ease;
    }

    .department-card:hover::before {
      opacity: 1;
      animation: floatParticles 6s linear infinite;
    }

    @keyframes floatParticles {
      0% { transform: translateY(0) translateX(0); }
      100% { transform: translateY(-100%) translateX(20%); }
    }
    /* ===== LUXURY SUMMARY SECTION ===== */
    .employee-summary {
      margin-top: 5rem;
      padding: 3rem 2rem;
      background: linear-gradient(135deg, rgba(30,30,30,0.95) 0%, rgba(18,18,18,0.98) 100%);
      border-radius: 20px;
      border: 1px solid rgba(212, 175, 55, 0.3);
      transform: translateY(30px);
      opacity: 0;
      transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
      box-shadow: 0 30px 60px rgba(0,0,0,0.5), 
                  inset 0 0 0 1px rgba(212, 175, 55, 0.1);
      position: relative;
      overflow: hidden;
    }

    .employee-summary.visible {
      transform: translateY(0);
      opacity: 1;
    }

    .employee-summary::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at 20% 30%, 
        rgba(212, 175, 55, 0.03) 0%, 
        transparent 50%);
      pointer-events: none;
    }

    .summary-header {
      text-align: center;
      margin-bottom: 3rem;
    }

    .summary-title {
      font-family: 'Cinzel Decorative', serif;
      font-size: 2.2rem;
      color: transparent;
      background: linear-gradient(135deg, var(--gold) 0%, var(--gold-light) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      margin-bottom: 1rem;
      letter-spacing: 0.05em;
    }

    .summary-divider {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1rem;
      margin: 0 auto;
      max-width: 400px;
    }

    .divider-line {
      height: 1px;
      flex-grow: 1;
      background: linear-gradient(90deg, 
        transparent 0%, 
        rgba(212, 175, 55, 0.5) 50%, 
        transparent 100%);
    }

    .divider-icon {
      color: var(--gold);
      font-size: 1.2rem;
      opacity: 0.8;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 2.5rem;
      margin-bottom: 3rem;
    }

    .summary-item {
      text-align: center;
      position: relative;
      padding: 1.5rem 1rem;
      background: rgba(20, 20, 20, 0.6);
      border-radius: 12px;
      border: 1px solid rgba(212, 175, 55, 0.1);
      transition: all 0.5s cubic-bezier(0.22, 1, 0.36, 1);
      transform: translateY(20px);
      opacity: 0;
    }

    .employee-summary.visible .summary-item {
      transform: translateY(0);
      opacity: 1;
    }

    .summary-item:hover {
      transform: translateY(-5px) scale(1.03) !important;
      background: rgba(25, 25, 25, 0.8);
      box-shadow: 0 15px 30px rgba(0,0,0,0.4),
                  0 0 0 1px rgba(212, 175, 55, 0.2);
      border-color: rgba(212, 175, 55, 0.3);
    }

    .summary-value {
      font-family: 'Cinzel', serif;
      font-size: 3rem;
      background: linear-gradient(135deg, var(--gold) 0%, var(--gold-light) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 0.5rem;
      line-height: 1;
      transition: all 0.3s ease;
    }

    .summary-item:hover .summary-value {
      transform: scale(1.1);
      text-shadow: 0 0 15px rgba(212, 175, 55, 0.4);
    }

    .summary-label {
      font-size: 0.95rem;
      color: var(--parchment);
      opacity: 0.8;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      margin-bottom: 1rem;
      transition: all 0.3s ease;
    }

    .summary-item:hover .summary-label {
      opacity: 1;
      color: var(--gold-light);
    }

    .summary-progress {
      height: 4px;
      background: rgba(212, 175, 55, 0.1);
      border-radius: 2px;
      overflow: hidden;
      margin: 0 auto;
      max-width: 150px;
    }

    .progress-bar {
      height: 100%;
      background: linear-gradient(90deg, 
        var(--gold-dark) 0%, 
        var(--gold) 50%, 
        var(--gold-light) 100%);
      transition: width 1.5s ease-out;
    }

    /* Salary Stats */
    .salary-stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1.5rem;
      margin-top: 2rem;
    }

    .salary-stat {
      background: linear-gradient(135deg, rgba(25,25,25,0.9) 0%, rgba(15,15,15,0.95) 100%);
      padding: 1.5rem;
      border-radius: 12px;
      border: 1px solid rgba(212, 175, 55, 0.15);
      transition: all 0.5s cubic-bezier(0.22, 1, 0.36, 1);
      display: flex;
      align-items: center;
      gap: 1.2rem;
      position: relative;
      overflow: hidden;
      transform: translateY(20px);
      opacity: 0;
    }

    .employee-summary.visible .salary-stat {
      transform: translateY(0);
      opacity: 1;
    }

    .salary-stat:hover {
      transform: translateY(-5px) scale(1.03) !important;
      box-shadow: 0 10px 30px rgba(0,0,0,0.4),
                  0 0 0 1px rgba(212, 175, 55, 0.25);
      border-color: rgba(212, 175, 55, 0.4);
    }

    .stat-icon {
      font-size: 1.8rem;
      color: var(--gold);
      min-width: 50px;
      height: 50px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(212, 175, 55, 0.1);
      border-radius: 50%;
      transition: all 0.3s ease;
    }

    .salary-stat:hover .stat-icon {
      transform: scale(1.2);
      background: rgba(212, 175, 55, 0.2);
      box-shadow: 0 0 15px rgba(212, 175, 55, 0.3);
    }

    .stat-content {
      flex-grow: 1;
    }

    .salary-stat-title {
      color: var(--gold);
      font-family: 'Cinzel', serif;
      font-size: 1rem;
      margin-bottom: 0.3rem;
      letter-spacing: 0.05em;
    }

    .salary-stat-value {
      font-size: 1.6rem;
      color: var(--ivory);
      font-weight: 500;
      letter-spacing: 0.05em;
    }

    .stat-sparkle {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      background: radial-gradient(circle at center, 
        rgba(212, 175, 55, 0.1) 0%, 
        transparent 70%);
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .salary-stat:hover .stat-sparkle {
      opacity: 1;
    }

    /* Animation Delays */
    .summary-item:nth-child(1) { transition-delay: 0.1s; }
    .summary-item:nth-child(2) { transition-delay: 0.2s; }
    .summary-item:nth-child(3) { transition-delay: 0.3s; }
    .summary-item:nth-child(4) { transition-delay: 0.4s; }

    .salary-stat:nth-child(1) { transition-delay: 0.5s; }
    .salary-stat:nth-child(2) { transition-delay: 0.6s; }
    .salary-stat:nth-child(3) { transition-delay: 0.7s; }
    .salary-stat:nth-child(4) { transition-delay: 0.8s; }

    .new-section {
      padding: 5rem 2rem;
      max-width: 1200px;
      margin: 0 auto;
      position: relative;
    }

    .section-header {
      text-align: center;
      margin-bottom: 3rem;
    }

    .section-title {
      font-family: 'Cinzel Decorative', serif;
      font-size: clamp(2rem, 5vw, 3.5rem);
      color: transparent;
      background: linear-gradient(135deg, var(--gold) 0%, var(--gold-light) 100%);
      -webkit-background-clip: text;
      background-clip: text;
      margin-bottom: 1.5rem;
    }

    .section-subtitle {
      font-family: 'Playfair Display', serif;
      color: var(--gold-light);
      font-size: 1.2rem;
      letter-spacing: 0.1em;
    }

    /* Interior Slideshow */
    .slideshow-container {
      position: relative;
      width: 100%;
      max-width: 900px;
      height: 500px; /* Fixed height */
      margin: 0 auto 3rem;
      overflow: hidden;
      border-radius: 12px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.8);
      border: 1px solid rgba(212,175,55,0.3);
    }

    #slideshow {
      position: relative;
      width: 100%;
      height: 100%;
    }
    .slide {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      opacity: 0;
      transition: opacity 0.8s ease;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 20px 50px rgba(0,0,0,0.8);
      border: 1px solid rgba(212,175,55,0.3);
    }

    .slide.active {
      opacity: 1;
    }


    .slide img {
      width: 100%;
      height: 100%;
      object-fit: cover; /* Ensures images fill container */
      object-position: center;
}
    .slide {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      opacity: 0;
      transition: opacity 1s ease-in-out;
    }

    .slide-nav {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      background: rgba(20,20,20,0.8);
      color: var(--gold);
      border: none;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      font-size: 1.5rem;
      cursor: pointer;
      z-index: 10;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 1px solid rgba(212,175,55,0.3);
    }

    #prev-slide { left: 20px; }
    #next-slide { right: 20px; }

    .slide-nav:hover {
      background: rgba(212,175,55,0.3);
      transform: translateY(-50%) scale(1.1);
    }

    .slide-indicators {
      position: absolute;
      bottom: 20px;
      left: 0;
      right: 0;
      display: flex;
      justify-content: center;
      gap: 10px;
      z-index: 10;
    }

    .indicator {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: rgba(255,255,255,0.3);
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .indicator.active {
      background: var(--gold);
      transform: scale(1.3);
    }

    /* Pricing Grid */
    .pricing-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }

    .pricing-card {
      background: linear-gradient(135deg, rgba(30,30,30,0.95) 0%, rgba(18,18,18,0.98) 100%);
      border-radius: 16px;
      padding: 2rem;
      border: 1px solid rgba(212,175,55,0.2);
      transition: all 0.4s ease;
    }

    .pricing-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 50px rgba(0,0,0,0.6);
      border-color: rgba(212,175,55,0.4);
    }

    .pricing-header {
      margin-bottom: 1.5rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid rgba(212,175,55,0.1);
    }

    .pricing-header h3 {
      font-family: 'Cinzel', serif;
      color: var(--gold);
      font-size: 1.5rem;
      margin-bottom: 0.5rem;
    }

    .price {
      font-size: 2.5rem;
      color: var(--ivory);
      font-weight: 600;
    }

    .price span {
      font-size: 1rem;
      color: var(--gold-light);
    }

    .pricing-features {
      list-style: none;
      margin: 1.5rem 0;
    }

    .pricing-features li {
      padding: 0.8rem 0;
      border-bottom: 1px dashed rgba(212,175,55,0.1);
      position: relative;
      padding-left: 1.5rem;
    }

    .pricing-features li::before {
      content: '•';
      color: var(--gold);
      position: absolute;
      left: 0;
    }

    .book-button {
      width: 100%;
      padding: 1rem;
      background: transparent;
      color: var(--gold);
      border: 2px solid var(--gold);
      border-radius: 50px;
      font-family: 'Cinzel', serif;
      cursor: pointer;
      transition: all 0.4s ease;
    }

    .book-button:hover {
      background: rgba(212,175,55,0.1);
      box-shadow: 0 0 20px rgba(212,175,55,0.3);
    }

    /* Location Section */
    .map-container {
      height: 400px;
      background: url('https://maps.googleapis.com/maps/api/staticmap?center=Loire+Valley,France&zoom=11&size=800x400&maptype=terrain&key=YOUR_API_KEY') center/cover no-repeat;
      border-radius: 12px;
      position: relative;
      margin-bottom: 3rem;
      border: 1px solid rgba(212,175,55,0.3);
    }

    .location-details {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-top: 2rem;
    }

    .detail-card {
      background: rgba(30,30,30,0.8);
      padding: 1.5rem;
      border-radius: 12px;
      text-align: center;
      border: 1px solid rgba(212,175,55,0.1);
    }

    .detail-icon {
      font-size: 2rem;
      margin-bottom: 1rem;
      color: var(--gold);
    }

    .detail-card h4 {
      font-family: 'Cinzel', serif;
      color: var(--gold-light);
      margin-bottom: 0.5rem;
    }

    /* Amenities Grid */
    .amenities-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-top: 3rem;
    }

    .amenity-card {
      background: rgba(30,30,30,0.8);
      padding: 2rem 1.5rem;
      border-radius: 12px;
      text-align: center;
      border: 1px solid rgba(212,175,55,0.1);
      transition: all 0.4s ease;
    }

    .amenity-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 40px rgba(0,0,0,0.5);
      border-color: rgba(212,175,55,0.3);
    }

    .amenity-icon {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: var(--gold);
    }

    .amenity-card h3 {
      font-family: 'Cinzel', serif;
      color: var(--gold-light);
      margin-bottom: 0.5rem;
    }

    img {
      max-width: 100%;
      height: auto;
      display: block;
    }

    .slide img {
      width: 100%;
      height: 500px;
      object-fit: cover;
      object-position: center;
    }

    .map-container {
      position: relative;
      overflow: hidden;
      padding-top: 56.25%;
      border-radius: 12px;
      border: 1px solid rgba(212,175,55,0.3);
    }

    .map-container iframe {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      border: none;
    }

    /* ===== CONCEPT SECTION STYLING ===== */
    .concept-section {
      padding: 8rem 2rem;
      position: relative;
      background: linear-gradient(
        to bottom,
        rgba(10, 10, 10, 0.9),
        rgba(5, 5, 5, 0.95)
      );
      overflow: hidden;
    }

    .concept-container {
      max-width: 1200px;
      margin: 0 auto;
      position: relative;
      padding: 4rem;
      border: 1px solid rgba(212, 175, 55, 0.2);
      border-radius: 16px;
      background: rgba(18, 18, 18, 0.7);
      backdrop-filter: blur(12px);
      box-shadow: 0 30px 60px rgba(0, 0, 0, 0.6),
        inset 0 0 0 1px rgba(212, 175, 55, 0.1);
      transform-style: preserve-3d;
      perspective: 1000px;
    }

    .concept-ornament {
      position: absolute;
      width: 120px;
      height: 120px;
      background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><path fill="%23d4af37" d="M50 0 L60 40 L100 50 L60 60 L50 100 L40 60 L0 50 L40 40 Z" opacity="0.3"/></svg>');
      background-size: contain;
      background-repeat: no-repeat;
      opacity: 0.3;
      transition: all 0.8s cubic-bezier(0.19, 1, 0.22, 1);
    }

    .concept-ornament.top-left {
      top: 20px;
      left: 20px;
    }

    .concept-ornament.top-right {
      top: 20px;
      right: 20px;
      transform: rotate(90deg);
    }

    .concept-ornament.bottom-left {
      bottom: 20px;
      left: 20px;
      transform: rotate(270deg);
    }

    .concept-ornament.bottom-right {
      bottom: 20px;
      right: 20px;
      transform: rotate(180deg);
    }

    .concept-content {
      position: relative;
      z-index: 2;
    }

    .concept-title {
      font-family: 'Cinzel Decorative', serif;
      font-weight: 700;
      text-align: center;
      margin-bottom: 4rem;
      color: white;
      background: linear-gradient(
        to right,
        white 0%,
        #f8f4e9 50%,
        white 100%
      );
      -webkit-background-clip: text;
      background-clip: text;
      text-shadow: 
        0 2px 10px rgba(0, 0, 0, 0.5),
        0 0 20px rgba(212, 175, 55, 0.4);
    }

    .title-line {
      display: block;
      font-size: clamp(2.5rem, 6vw, 4.5rem);
      line-height: 1.2;
      letter-spacing: 0.1em;
      transform: translateY(30px);
      opacity: 0;
      animation: fadeInUp 1s cubic-bezier(0.22, 1, 0.36, 1) forwards;
      transition: all 0.3s ease;
    }

    .concept-title:hover .title-line {
      text-shadow: 
        0 0 20px rgba(212, 175, 55, 0.8),
        0 0 40px rgba(212, 175, 55, 0.3);
    }

    .title-line:nth-child(1) {
      animation-delay: 0.2s;
    }

    .title-line:nth-child(2) {
      animation-delay: 0.4s;
    }

    .title-line:nth-child(3) {
      animation-delay: 0.6s;
    }

    .concept-text-wrapper {
      max-width: 800px;
      margin: 0 auto 5rem;
      position: relative;
    }

    .concept-text {
      font-family: 'EB Garamond', serif;
      font-size: clamp(1.2rem, 3vw, 1.5rem);
      line-height: 1.8;
      color: var(--parchment);
      text-align: center;
      position: relative;
      padding: 2rem;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeInUp 1s cubic-bezier(0.22, 1, 0.36, 1) 0.8s forwards;
    }

    .concept-text::before,
    .concept-text::after {
      content: '"';
      position: absolute;
      font-family: 'Cinzel', serif;
      font-size: 4rem;
      color: var(--gold);
      opacity: 0.5;
      line-height: 1;
    }

    .concept-text::before {
      top: 0;
      left: 0;
    }

    .concept-text::after {
      bottom: -1.5rem;
      right: 0;
    }

    .concept-highlights {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin-top: 4rem;
    }

    .highlight-item {
      background: rgba(25, 25, 25, 0.6);
      border: 1px solid rgba(212, 175, 55, 0.1);
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      transition: all 0.6s cubic-bezier(0.22, 1, 0.36, 1);
      transform: translateY(50px);
      opacity: 0;
      animation: fadeInUp 0.8s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }

    .highlight-item:nth-child(1) {
      animation-delay: 1s;
    }

    .highlight-item:nth-child(2) {
      animation-delay: 1.2s;
    }

    .highlight-item:nth-child(3) {
      animation-delay: 1.4s;
    }

    .highlight-item:hover {
      transform: translateY(-10px) scale(1.03);
      background: rgba(30, 30, 30, 0.8);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4),
        0 0 0 1px rgba(212, 175, 55, 0.3);
      border-color: rgba(212, 175, 55, 0.4);
    }

    .highlight-icon {
      font-size: 3rem;
      margin-bottom: 1.5rem;
      display: inline-block;
      transition: all 0.5s ease;
    }

    .highlight-item:hover .highlight-icon {
      transform: scale(1.2) rotate(10deg);
      text-shadow: 0 0 15px rgba(212, 175, 55, 0.5);
    }

    .highlight-title {
      font-family: 'Cinzel', serif;
      color: var(--gold);
      font-size: 1.5rem;
      margin-bottom: 1rem;
      letter-spacing: 0.05em;
      transition: all 0.3s ease;
    }

    .highlight-item:hover .highlight-title {
      color: var(--gold-light);
      letter-spacing: 0.1em;
    }

    .highlight-text {
      font-family: 'EB Garamond', serif;
      color: var(--parchment);
      line-height: 1.6;
      opacity: 0.9;
      transition: all 0.3s ease;
    }

    .highlight-item:hover .highlight-text {
      opacity: 1;
    }

    /* ===== CLASSIFICATION SECTION ===== */
    .classification-section {
      background: linear-gradient(to bottom, rgba(15,15,15,0.95), rgba(10,10,10,0.98));
      padding: 6rem 2rem;
      text-align: center;
    }

    .classification-badge {
      background: rgba(212,175,55,0.1);
      border: 2px solid rgba(212,175,55,0.3);
      border-radius: 50px;
      padding: 1.5rem 2rem;
      display: inline-flex;
      align-items: center;
      gap: 1rem;
      backdrop-filter: blur(5px);
      transition: all 0.5s ease;
      max-width: max-content;
    }

    .classification-badge-container {
      display: flex;
      justify-content: center;
      margin-bottom: 3rem;
      width: 100%;
    }

    .classification-badge:hover {
      transform: scale(1.05);
      background: rgba(212,175,55,0.2);
      box-shadow: 0 0 30px rgba(212,175,55,0.2);
    }

    .badge-icon {
      font-size: 2rem;
    }

    .badge-text {
      font-family: 'Cinzel', serif;
      font-size: 1.8rem;
      color: var(--gold);
      letter-spacing: 0.1em;
    }

    .classification-features {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      max-width: 900px;
      margin: 0 auto;
    }

    .feature-card {
      background: rgba(30,30,30,0.7);
      border: 1px solid rgba(212,175,55,0.1);
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      transition: all 0.5s cubic-bezier(0.22, 1, 0.36, 1);
    }

    .feature-card:hover {
      transform: translateY(-10px);
      border-color: rgba(212,175,55,0.4);
      box-shadow: 0 20px 40px rgba(0,0,0,0.4);
    }

    .feature-icon {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      display: inline-block;
      transition: all 0.5s ease;
    }

    .feature-card:hover .feature-icon {
      transform: scale(1.2) rotate(10deg);
      text-shadow: 0 0 15px rgba(212,175,55,0.5);
    }

    /* ===== TARGET MARKET SECTION ===== */
    .target-section {
      background: linear-gradient(to bottom, rgba(18,18,18,0.95), rgba(12,12,12,0.98));
      padding: 6rem 2rem;
    }

    .target-profile {
      text-align: center;
      margin-bottom: 4rem;
    }

    .age-range {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .age-bubble {
      width: 70px;
      height: 70px;
      border-radius: 50%;
      background: rgba(212,175,55,0.1);
      border: 2px solid var(--gold);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Cinzel', serif;
      font-size: 1.8rem;
      color: var(--gold-light);
      transition: all 0.5s ease;
    }

    .age-line {
      flex-grow: 1;
      height: 2px;
      background: linear-gradient(to right, var(--gold), transparent);
      max-width: 200px;
      position: relative;
    }

    .age-line::after {
      content: '';
      position: absolute;
      top: 0;
      right: 0;
      bottom: 0;
      left: 0;
      background: linear-gradient(to left, var(--gold), transparent);
    }

    .guest-types {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      max-width: 900px;
      margin: 0 auto;
    }

    .guest-card {
      background: rgba(25,25,25,0.8);
      border: 1px solid rgba(212,175,55,0.1);
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      transition: all 0.5s ease;
    }

    .guest-card:hover {
      transform: translateY(-8px) scale(1.03);
      border-color: rgba(212,175,55,0.4);
      box-shadow: 0 15px 40px rgba(0,0,0,0.5);
    }

    .guest-icon {
      font-size: 2rem;
      margin-bottom: 1rem;
      transition: all 0.5s ease;
    }

    .guest-card:hover .guest-icon {
      transform: scale(1.3);
      filter: drop-shadow(0 0 8px rgba(212,175,55,0.5));
    }

    /* ===== DISCOUNTS SECTION ===== */
    .discounts-section {
      background: linear-gradient(to bottom, rgba(20,20,20,0.95), rgba(15,15,15,0.98));
      padding: 6rem 2rem;
    }

    .discounts-content {
      max-width: 900px;
      margin: 0 auto;
    }

    .discount-card {
      background: linear-gradient(135deg, rgba(30,30,30,0.9), rgba(20,20,20,0.95));
      border: 1px solid rgba(212,175,55,0.2);
      border-radius: 16px;
      padding: 3rem;
      text-align: center;
      position: relative;
      overflow: hidden;
      margin-bottom: 2rem;
      transition: all 0.6s cubic-bezier(0.22, 1, 0.36, 1);
    }

    .discount-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 25px 60px rgba(0,0,0,0.6);
      border-color: rgba(212,175,55,0.5);
    }

    .discount-badge {
      position: absolute;
      top: -20px;
      right: -20px;
      background: var(--gold);
      color: var(--black);
      width: 120px;
      height: 120px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Cinzel', serif;
      font-size: 1.8rem;
      font-weight: 700;
      transform: rotate(15deg);
      box-shadow: 0 10px 30px rgba(212,175,55,0.4);
    }

    .membership-benefits {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
    }

    .benefit-card {
      background: rgba(25,25,25,0.8);
      border: 1px solid rgba(212,175,55,0.1);
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      transition: all 0.5s ease;
    }

    .benefit-card:hover {
      transform: translateY(-5px);
      border-color: rgba(212,175,55,0.4);
      box-shadow: 0 15px 40px rgba(0,0,0,0.4);
    }

    .benefit-icon {
      font-size: 2rem;
      margin-bottom: 1rem;
      transition: all 0.5s ease;
    }

    .benefit-card:hover .benefit-icon {
      transform: scale(1.3);
      text-shadow: 0 0 15px rgba(212,175,55,0.5);
    }

    .offer-dates {
      font-family: 'EB Garamond', serif;
      font-style: italic;
      color: var(--gold-light);
      margin-top: 1rem;
      letter-spacing: 0.05em;
    }

    /* ===== SHARED STYLES ===== */
    .section-container {
      max-width: 1200px;
      margin: 0 auto;
    }

    .section-header {
      text-align: center;
      margin-bottom: 4rem;
    }

    .section-title {
      font-family: 'Cinzel Decorative', serif;
      color: white;
      font-size: clamp(2rem, 5vw, 3.5rem);
      margin-bottom: 1.5rem;
      line-height: 1.3;
    }

    .title-line {
      display: block;
    }

    .section-divider {
      width: 100px;
      height: 2px;
      background: linear-gradient(to right, transparent, var(--gold), transparent);
      margin: 0 auto;
      position: relative;
    }

    .section-divider::after {
      content: '';
      position: absolute;
      top: -5px;
      left: 50%;
      transform: translateX(-50%);
      width: 10px;
      height: 10px;
      background: var(--gold);
      border-radius: 50%;
      box-shadow: 0 0 15px var(--gold);
    }

    /* Animations */
    @keyframes floatUp {
      0% { transform: translateY(30px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }

    .section-header,
    .classification-badge,
    .feature-card,
    .target-profile,
    .guest-card,
    .discount-card,
    .benefit-card {
      animation: floatUp 1s cubic-bezier(0.22, 1, 0.36, 1) forwards;
      opacity: 0;
    }

    .feature-card:nth-child(1) { animation-delay: 0.2s; }
    .feature-card:nth-child(2) { animation-delay: 0.4s; }
    .feature-card:nth-child(3) { animation-delay: 0.6s; }
    .guest-card:nth-child(1) { animation-delay: 0.3s; }
    .guest-card:nth-child(2) { animation-delay: 0.5s; }
    .guest-card:nth-child(3) { animation-delay: 0.7s; }
    .benefit-card:nth-child(1) { animation-delay: 0.4s; }
    .benefit-card:nth-child(2) { animation-delay: 0.6s; }

    </style>
</head>
<body>

<header class="header">
  <h1>The Enchanted Bastion</h1>
</header>
<div class="starfield" id="starfield"></div>
 
<section class="hero-section" data-component="hero">
  <div class="hero-overlay"></div>
  
  <div class="hero-content">
    <h1 class="hero-title">The Enchanted Bastion</h1>
    <p class="hero-subtitle">LOIRE VALLEY, FRANCE</p>
    
    <div class="hero-image-container">
      <div class="hero-image-wrapper" id="heroImageWrapper">
        <img src="https://ik.imagekit.io/xeqk7r0ly/Firefly%20Create%20a%20photorealistic%208K%20sunset%20view%20of%20a%20luxurious%2012th-century%20castle%20hotel%20in%20France's%20.jpg" 
             alt="Couldn't load image" 
             class="hero-image">
        <div class="image-reflection"></div>
      </div>
    </div>
    
    <a href="#interior" class="hero-cta">Explore</a>
  </div>
</section>
  

<!-- ===== INTERIOR SECTION ===== -->
<section class="new-section" id="interior">
  <div class="section-header">
    <h2 class="section-title">Interior</h2>
    <p class="section-subtitle">LUXURIOUS ACCOMMODATIONS</p>
  </div>
  
  <div class="slideshow-container">
    <div id="slideshow">
      <div class="slide active">
        <img src="https://media-hosting.imagekit.io/d645b533bc994e5b/screenshot_1743008709723.png?Expires=1837616710&Key-Pair-Id=K2ZIVPTIP2VGHC&Signature=g7hUoEjRWAVYmIvGTOa3OpW-KmeE3xgIi4-TXaRTfGFoV88RuCq~WZjaXLeW8rW4JTZX-DDd9oDJ7LdRPuhDGyl0Y~xM8AaSSn33othQkjk8Z1qb-OYfthhN9Tcbo5yqNfiqCQ3rurl59BS-bRIAGBM6ZHWY9ahvr2B2ByDJW~EnAq1uo7h14cdGqnsp3iZVT9w3JzVsfBQsbvTZJFAmtzGImRLwKU6rzSZvFFGsv3Facyja95nHhlyR9ziik57hclFX90uzaUZ2tHFvIylyFOkpsSDRc2faNN6L0AWiXcS8YR-n6J9oBWoHOHRXoivXk8R4J7RZB68HyA~ycjlTSA__" alt="Grand Hall">
      </div>
      <div class="slide">
        <img src="https://media-hosting.imagekit.io/b38456ff27484b88/screenshot_1743008804722.png?Expires=1837616805&Key-Pair-Id=K2ZIVPTIP2VGHC&Signature=Y1D1WPWmZWax7~lArR6wOSRccBYx8TdUUqeHVZAES8a~~PvRaIeZeq~GSRwn5ydeKwz4SugqIVFI5Mn6xDDGnllxYbEuE76TJwlvamrQesWnciMkBlciWP~0DQFFDH0pTvbvNti0AIm8OZp~ThN08h2kwzlU5Omo~O7RyMfHP7rKDKTCyS-HiZCMjPtubDN~giWG0gzHZX4fV8d-6D3lV9UBBndwMVoSRnBAFtge4F31dPc9v~lpBJ~WH58V-W7xsA0cnbYCqlf4NTvVKqMJBvWL6gqTnAFePF4voyO5m2cWrL8vI4p1Vumv2dNKodOIkWlkw1mQpBsC~lI1eE5ohw__" alt="Royal Suite">
      </div>
      <div class="slide">
        <img src="https://media-hosting.imagekit.io/5300e5f1e0854cdc/screenshot_1743008624961.png?Expires=1837616625&Key-Pair-Id=K2ZIVPTIP2VGHC&Signature=SoxJo929BRQro7uKYBtCeWdEFo-5Px4ZpvqmhCDk2i6K32zi4XS7YRBkB0Kzn8Olv4KQrC8ELw5gwU3qvXLKULN5PbKX7Tw4ZIvNmqe2HKX3eFo4paZYcI27-euBt4YfZZtOG3CJLwWoYIRQ5fu5LoGVcv77cMBbZVdHAo17wn7GgU35avQRYBfQfCmZhjlBTZqhzSXLNZ79RYouIJU-Mi3cLK1joe3tkBDW9fiJIwOwaJHPGYg~kj7sDACneSpLgIk8SdchVMjTlOoeDYtqtc2cgBitjpgSVfzSPdNl0Y8Dbz4Dj9Dp-c1koto~woX88I~W8YquFPnMEdquCkVnOA__" alt="Library">
      </div>
    </div>
    <button class="slide-nav" id="prev-slide">❮</button>
    <button class="slide-nav" id="next-slide">❯</button>
    <div class="slide-indicators"></div>
  </div>
</section>

<section class="new-section" id="pricing">
  <div class="section-header">
    <h2 class="section-title">Pricing</h2>
    <p class="section-subtitle">EXCLUSIVE PACKAGES</p>
  </div>
  
  <div class="pricing-grid">
    <div class="pricing-card">
      <div class="pricing-header">
        <h3>Standard Chamber</h3>
        <div class="price">€299<span>/night</span></div>
      </div>
      <ul class="pricing-features">
        <li>Authentic medieval quarters</li>
        <li>Four-poster bed with linen drapes</li>
        <li>Private garderobe</li>
        <li>Daily turndown service</li>
      </ul>
      <button class="book-button">Reserve Now</button>
    </div>
    
    <div class="pricing-card">
      <div class="pricing-header">
        <h3>Noble Suite</h3>
        <div class="price">€550<span>/night</span></div>
      </div>
      <ul class="pricing-features">
        <li>Tower room with panoramic views</li>
        <li>Canopied king bed</li>
        <li>Marble bathing chamber</li>
        <li>Personal steward</li>
      </ul>
      <button class="book-button">Reserve Now</button>
    </div>
    
    <div class="pricing-card">
      <div class="pricing-header">
        <h3>Royal Suite</h3>
        <div class="price">€950<span>/night</span></div>
      </div>
      <ul class="pricing-features">
        <li>Private turret suite</li>
        <li>Dressing chamber with vanity</li>
        <li>24/7 butler service</li>
        <li>Complimentary champagne</li>
      </ul>
      <button class="book-button">Reserve Now</button>
    </div>
  </div>
</section>

<!-- ===== LOCATION SECTION ===== -->
<section class="new-section" id="location">
  <div class="section-header">
    <h2 class="section-title">Location</h2>
    <p class="section-subtitle">LOIRE VALLEY, FRANCE</p>
  </div>
  
  <div class="map-container">
    <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d87890.57171999585!2d0.3255239999999999!3d47.394144!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x47fcd59fb2d6f2a5%3A0x427d00a4af1a3b00!2sLoire%20Valley%2C%20France!5e0!3m2!1sen!2sus!4v1712345678901" width="100%" height="100%" style="border:0; border-radius: 12px;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
  </div>
  
  <div class="location-details">
    <div class="detail-card">
      <div class="detail-icon">✈️</div>
      <h4>Nearest Airport</h4>
      <p>Tours Val de Loire (25km)</p>
    </div>
    <div class="detail-card">
      <div class="detail-icon">🚂</div>
      <h4>Train Station</h4>
      <p>Saint-Pierre-des-Corps (18km)</p>
    </div>
    <div class="detail-card">
      <div class="detail-icon">🍷</div>
      <h4>Vineyards</h4>
      <p>15+ within 10km radius</p>
    </div>
  </div>
</section>

<!-- ===== AMENITIES SECTION ===== -->
<section class="new-section" id="amenities">
  <div class="section-header">
    <h2 class="section-title">Amenities</h2>
    <p class="section-subtitle">UNPARALLELED COMFORTS</p>
  </div>
  
  <div class="amenities-grid">
    <div class="amenity-card">
      <div class="amenity-icon">🛁</div>
      <h3>Armory Spa</h3>
      <p>Ancient herbal treatments in a converted armory</p>
    </div>
    <div class="amenity-card">
      <div class="amenity-icon">🍽️</div>
      <h3>Great Hall Dining</h3>
      <p>5-course medieval-inspired tasting menus</p>
    </div>
    <div class="amenity-card">
      <div class="amenity-icon">🏹</div>
      <h3>Archery Range</h3>
      <p>Professional instruction available</p>
    </div>
  </div>
</section>

<!-- ===== CONCEPT SECTION ===== -->
<section class="concept-section">
  <div class="concept-container">
    <!-- Decorative elements -->
    <div class="concept-ornament top-left"></div>
    <div class="concept-ornament top-right"></div>
    <div class="concept-ornament bottom-left"></div>
    <div class="concept-ornament bottom-right"></div>
    
    <!-- Main content -->
    <div class="concept-content">
      <h2 class="concept-title">
        <span class="title-line">Concept</span>
      </h2>
      
      <div class="concept-text-wrapper">
        <p class="concept-text">
          We resurrect the authentic soul of medieval Europe through a painstakingly restored 12th-century castle, transformed into a luxury hospitality experience where history breathes and modernity whispers.
        </p>
      </div>
      
      <div class="concept-highlights">
        <div class="highlight-item">
          <div class="highlight-icon">🏰</div>
          <h3 class="highlight-title">Historical</h3>
          <p class="highlight-text">Every stone tells a story of centuries past</p>
        </div>
        
        <div class="highlight-item">
          <div class="highlight-icon">✨</div>
          <h3 class="highlight-title">Modern Luxury</h3>
          <p class="highlight-text">Contemporary comforts woven seamlessly into ancient walls</p>
        </div>
        
        <div class="highlight-item">
          <div class="highlight-icon">🕯️</div>
          <h3 class="highlight-title">Timeless Atmosphere</h3>
          <p class="highlight-text">Where the past and present dance in golden light</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ===== CLASSIFICATION SECTION ===== -->
<section class="classification-section">
  <div class="section-container">
    <div class="section-header">
      <h2 class="section-title">
        <span class="title-line">Hotel</span>
        <span class="title-line">Classification</span>
      </h2>
      <div class="section-divider"></div>
    </div>
    
    <div class="classification-content">
      <div class="classification-badge-container">
        <div class="classification-badge">
          <div class="badge-icon">🏆</div>
          <div class="badge-text">Full Service</div>
        </div>
      </div>
      
      <div class="classification-features">
        <div class="feature-card">
          <div class="feature-icon">🛎️</div>
          <h3>24/7 Concierge</h3>
          <p>Round-the-clock personalized service</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">🍽️</div>
          <h3>Gourmet Dining</h3>
          <p>3 award-winning restaurants</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">🏊</div>
          <h3>Wellness Center</h3>
          <p>Spa, pool & fitness facilities</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ===== TARGET MARKET SECTION ===== -->
<section class="target-section">
  <div class="section-container">
    <div class="section-header">
      <h2 class="section-title">
        <span class="title-line">Our</span>
        <span class="title-line">Distinguished Guests</span>
      </h2>
      <div class="section-divider"></div>
    </div>
    
    <div class="target-content">
      <div class="target-profile">
        <div class="age-range">
          <div class="age-bubble">25</div>
          <div class="age-line"></div>
          <div class="age-bubble">55</div>
        </div>
        <h3>Primary Demographic</h3>
      </div>
      
      <div class="guest-types">
        <div class="guest-card">
          <div class="guest-icon">💎</div>
          <h4>Luxury Seekers</h4>
          <p>Discerning travelers expecting premium service</p>
        </div>
        
        <div class="guest-card">
          <div class="guest-icon">🏰</div>
          <h4>History Enthusiasts</h4>
          <p>Guests fascinated by medieval heritage</p>
        </div>
        
        <div class="guest-card">
          <div class="guest-icon">✨</div>
          <h4>Experience Collectors</h4>
          <p>Those seeking unique, memorable stays</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ===== DISCOUNTS SECTION ===== -->
<section class="discounts-section">
  <div class="section-container">
    <div class="section-header">
      <h2 class="section-title">
        <span class="title-line">Exclusive</span>
        <span class="title-line">Benefits</span>
      </h2>
      <div class="section-divider"></div>
    </div>
    
    <div class="discounts-content">
      <div class="discount-card main-offer">
        <div class="discount-badge">40% OFF</div>
        <h3>Seasonal Promotions</h3>
        <p>Enjoy substantial savings during selected periods</p>
        <div class="offer-dates">January - March | September - November</div>
      </div>
      
      <div class="membership-benefits">
        <div class="benefit-card">
          <div class="benefit-icon">👑</div>
          <h4>Royal Member Deals</h4>
          <p>Additional 15% off for loyalty members</p>
        </div>
        
        <div class="benefit-card">
          <div class="benefit-icon">🎁</div>
          <h4>Early Booking Bonus</h4>
          <p>Book 90+ days in advance for extra perks</p>
        </div>
      </div>
    </div>
  </div>
</section>
  <!-- ===== MODULAR JAVASCRIPT ===== -->
<script>
  // Create twinkling stars
  function createStars() {
    const starfield = document.getElementById('starfield');
    const starCount = 200;

    for (let i = 0; i < starCount; i++) {
      const star = document.createElement('div');
      star.classList.add('star');
  
      // Random properties
      const size = Math.random() * 2 + 0.5;
      const x = Math.random() * 100;
      const y = Math.random() * 100;
      const opacity = Math.random() * 0.7 + 0.1;
      const duration = Math.random() * 5 + 3;
      const delay = Math.random() * 5;
  
      star.style.cssText = `
        width: ${size}px;
        height: ${size}px;
        left: ${x}%;
        top: ${y}%;
        --opacity: ${opacity};
        --duration: ${duration}s;
        animation-delay: ${delay}s;
      `;
  
      starfield.appendChild(star);
    }
  }

  // Initialize animations for staff section
  function initStaffAnimations() {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.section-header, .department-card, .employee-summary').forEach(el => {
      observer.observe(el);
    });
  }

  // Start everything when page loads
  document.addEventListener('DOMContentLoaded', () => {
    createStars();
    initStaffAnimations();
    
    // Initialize hero parallax effect
    const hero = document.querySelector('.hero-section');
    if (hero) {
      const wrapper = hero.querySelector('#heroImageWrapper');
      const imageContainer = hero.querySelector('.hero-image-container');
      
      if (wrapper && imageContainer) {
        hero.addEventListener('mousemove', (e) => {
          // Only tilt when mouse is over the image container
          const rect = imageContainer.getBoundingClientRect();
          const x = e.clientX - rect.left;
          const y = e.clientY - rect.top;
          
          // Check if mouse is within image bounds
          if (x > 0 && x < rect.width && y > 0 && y < rect.height) {
            const centerX = rect.width / 2;
            const centerY = rect.height / 2;
            const angleX = (y - centerY) / 30; // Reduced sensitivity
            const angleY = (centerX - x) / 30; // Reduced sensitivity
            
            wrapper.style.transform = `rotateX(${angleX}deg) rotateY(${angleY}deg)`;
            wrapper.style.transition = 'transform 0.1s linear';
          } else {
            wrapper.style.transform = 'rotateX(0) rotateY(0)';
            wrapper.style.transition = 'transform 0.5s cubic-bezier(0.18, 0.89, 0.32, 1.28)';
          }
        });
        
        hero.addEventListener('mouseleave', () => {
          wrapper.style.transform = 'rotateX(0) rotateY(0)';
          wrapper.style.transition = 'transform 0.5s cubic-bezier(0.18, 0.89, 0.32, 1.28)';
        });
      }
    }
    
    // Smooth scrolling for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        e.preventDefault();
        document.querySelector(this.getAttribute('href')).scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      });
    });
    
    // Remove preloader after delay
    const preloader = document.querySelector('.preloader');
    if (preloader) {
      setTimeout(() => {
        preloader.classList.add('fade-out');
        setTimeout(() => preloader.remove(), 1000);
      }, 1500);
    }
  });

// Enhanced Slideshow Functionality
document.addEventListener('DOMContentLoaded', function() {
  const slides = document.querySelectorAll('.slide');
  const prevBtn = document.getElementById('prev-slide');
  const nextBtn = document.getElementById('next-slide');
  const indicatorsContainer = document.querySelector('.slide-indicators');
  let currentIndex = 0;
  let slideInterval;

  // Create indicators
  slides.forEach((_, index) => {
    const indicator = document.createElement('div');
    indicator.classList.add('indicator');
    if (index === 0) indicator.classList.add('active');
    indicator.addEventListener('click', () => goToSlide(index));
    indicatorsContainer.appendChild(indicator);
  });

  const indicators = document.querySelectorAll('.indicator');

  function updateSlides() {
    slides.forEach((slide, index) => {
      slide.classList.toggle('active', index === currentIndex);
    });
    
    indicators.forEach((indicator, index) => {
      indicator.classList.toggle('active', index === currentIndex);
    });
  }

  function nextSlide() {
    currentIndex = (currentIndex + 1) % slides.length;
    updateSlides();
  }

  function prevSlide() {
    currentIndex = (currentIndex - 1 + slides.length) % slides.length;
    updateSlides();
  }

  function goToSlide(index) {
    currentIndex = index;
    updateSlides();
  }

  function startSlideshow() {
    slideInterval = setInterval(nextSlide, 5000);
  }

  function stopSlideshow() {
    clearInterval(slideInterval);
  }

  // Event listeners
  nextBtn.addEventListener('click', () => {
    nextSlide();
    stopSlideshow();
    startSlideshow();
  });

  prevBtn.addEventListener('click', () => {
    prevSlide();
    stopSlideshow();
    startSlideshow();
  });

  // Start slideshow
  startSlideshow();

  // Pause on hover
  const slideshowContainer = document.querySelector('.slideshow-container');
  slideshowContainer.addEventListener('mouseenter', stopSlideshow);
  slideshowContainer.addEventListener('mouseleave', startSlideshow);
});

</script>

<!-- Staff Section -->
<section class="staff-section" id="staff">
  <div class="staff-container">
    <div class="section-header">
      <h2 class="section-title">Our Royal Attendants</h2>
      <p class="section-subtitle">136 dedicated professionals serving your every need</p>
    </div>
    
    <div class="employee-structure">
      <div class="department-card">
        <div class="card-glow"></div>
        <div class="department-header">
          <div class="department-icon">🏨</div>
          <div class="department-content">
            <h3 class="department-title">Front Office</h3>
            <div class="employee-count">24 Staff</div>
          </div>
        </div>
        <ul class="employee-list">
          <li class="employee-item">
            <span class="position">Front Desk Manager</span>
            <div class="employee-meta">
              <span class="count">2</span>
              <span class="salary">€65,000</span>
            </div>
          </li>
          <li class="employee-item">
            <span class="position">Front Desk Agents</span>
            <div class="employee-meta">
              <span class="count">14</span>
              <span class="salary">€42,000</span>
            </div>
          </li>
          <li class="employee-item">
            <span class="position">Concierge</span>
            <div class="employee-meta">
              <span class="count">4</span>
              <span class="salary">€48,000</span>
            </div>
          </li>
        </ul>
      </div>
      <!-- Housekeeping -->
      <div class="department-card">
        <div class="department-header">
          <div class="department-icon">🧹</div>
          <div class="department-title">Housekeeping</div>
          <div class="employee-count">42 Staff</div>
        </div>
        <ul class="employee-list">
          <li class="employee-item">
            <span class="position">Executive Housekeeper</span>
            <span class="count">1</span>
            <span class="salary">€58,000</span>
          </li>
          <li class="employee-item">
            <span class="position">Room Attendants</span>
            <span class="count">32</span>
            <span class="salary">€36,000</span>
          </li>
          <li class="employee-item">
            <span class="position">Laundry Staff</span>
            <span class="count">4</span>
            <span class="salary">€34,000</span>
          </li>
        </ul>
      </div>
      
      <!-- Food & Beverage -->
      <div class="department-card">
        <div class="department-header">
          <div class="department-icon">🍽️</div>
          <div class="department-title">Food & Beverage</div>
          <div class="employee-count">28 Staff</div>
        </div>
        <ul class="employee-list">
          <li class="employee-item">
            <span class="position">Executive Chef</span>
            <span class="count">1</span>
            <span class="salary">€85,000</span>
          </li>
          <li class="employee-item">
            <span class="position">Sous Chefs</span>
            <span class="count">3</span>
            <span class="salary">€62,000</span>
          </li>
          <li class="employee-item">
            <span class="position">Bartenders</span>
            <span class="count">4</span>
            <span class="salary">€45,000</span>
          </li>
        </ul>
      </div>
      
      <!-- Guest Services -->
      <div class="department-card">
        <div class="department-header">
          <div class="department-icon">✨</div>
          <div class="department-title">Guest Services</div>
          <div class="employee-count">24 Staff</div>
        </div>
        <ul class="employee-list">
          <li class="employee-item">
            <span class="position">Spa Therapists</span>
            <span class="count">6</span>
            <span class="salary">€52,000</span>
          </li>
          <li class="employee-item">
            <span class="position">Activity Coordinators</span>
            <span class="count">4</span>
            <span class="salary">€45,000</span>
          </li>
          <li class="employee-item">
            <span class="position">Specialty Attendants</span>
            <span class="count">4</span>
            <span class="salary">€42,000</span>
          </li>
        </ul>
      </div>
      
      <!-- Maintenance -->
      <div class="department-card">
        <div class="department-header">
          <div class="department-icon">🔧</div>
          <div class="department-title">Maintenance</div>
          <div class="employee-count">10 Staff</div>
        </div>
        <ul class="employee-list">
          <li class="employee-item">
            <span class="position">Chief Engineer</span>
            <span class="count">1</span>
            <span class="salary">€72,000</span>
          </li>
          <li class="employee-item">
            <span class="position">Technicians</span>
            <span class="count">5</span>
            <span class="salary">€52,000</span>
          </li>
        </ul>
      </div>
      
      <!-- Security -->
      <div class="department-card">
        <div class="department-header">
          <div class="department-icon">🛡️</div>
          <div class="department-title">Security</div>
          <div class="employee-count">8 Staff</div>
        </div>
        <ul class="employee-list">
          <li class="employee-item">
            <span class="position">Security Director</span>
            <span class="count">1</span>
            <span class="salary">€75,000</span>
          </li>
          <li class="employee-item">
            <span class="position">Security Officers</span>
            <span class="count">7</span>
            <span class="salary">€55,000</span>
          </li>
        </ul>
      </div>
    </div>
  </div>
  <!-- Enhanced Staff Summary -->
<div class="employee-summary">
  <div class="summary-header">
    <h3 class="summary-title">Staffing Excellence</h3>
    <div class="summary-divider">
      <div class="divider-line"></div>
      <div class="divider-icon">✨</div>
      <div class="divider-line"></div>
    </div>
  </div>
  
  <div class="summary-grid">
    <div class="summary-item" data-delay="100">
      <div class="summary-value" id="employeeRatio">1.56</div>
      <div class="summary-label">Employees Per Room</div>
      <div class="summary-progress">
        <div class="progress-bar" style="width: 78%"></div>
      </div>
    </div>
    
    <div class="summary-item" data-delay="200">
      <div class="summary-value" id="totalEmployees">136</div>
      <div class="summary-label">Total Staff Members</div>
      <div class="summary-progress">
        <div class="progress-bar" style="width: 92%"></div>
      </div>
    </div>
    
    <div class="summary-item" data-delay="300">
      <div class="summary-value" id="serviceCoverage">24/7</div>
      <div class="summary-label">Service Coverage</div>
      <div class="summary-progress">
        <div class="progress-bar" style="width: 100%"></div>
      </div>
    </div>
    
    <div class="summary-item" data-delay="400">
      <div class="summary-value" id="guestRatio">3:1</div>
      <div class="summary-label">Guest to Staff Ratio</div>
      <div class="summary-progress">
        <div class="progress-bar" style="width: 85%"></div>
      </div>
    </div>
  </div>
  
  <div class="salary-stats">
    <div class="salary-stat" data-delay="500">
      <div class="stat-icon">💰</div>
      <div class="stat-content">
        <div class="salary-stat-title">Average Salary</div>
        <div class="salary-stat-value">€48,750</div>
      </div>
      <div class="stat-sparkle"></div>
    </div>
    
    <div class="salary-stat" data-delay="550">
      <div class="stat-icon">🏦</div>
      <div class="stat-content">
        <div class="salary-stat-title">Annual Payroll</div>
        <div class="salary-stat-value">€6.63M</div>
      </div>
      <div class="stat-sparkle"></div>
    </div>
    
    <div class="salary-stat" data-delay="600">
      <div class="stat-icon">🛡️</div>
      <div class="stat-content">
        <div class="salary-stat-title">Benefits Budget</div>
        <div class="salary-stat-value">€1.2M</div>
      </div>
      <div class="stat-sparkle"></div>
    </div>
    
    <div class="salary-stat" data-delay="650">
      <div class="stat-icon">🎓</div>
      <div class="stat-content">
        <div class="salary-stat-title">Training Budget</div>
        <div class="salary-stat-value">€350K</div>
      </div>
      <div class="stat-sparkle"></div>
    </div>
  </div>
</div>
</section>

<script>
  // Custom Gold Ring Cursor
  document.addEventListener('DOMContentLoaded', () => {
    const cursorRing = document.createElement('div');
    const cursorDot = document.createElement('div');
    cursorRing.classList.add('cursor-ring');
    cursorDot.classList.add('cursor-dot');
    document.body.appendChild(cursorRing);
    document.body.appendChild(cursorDot);
    
    document.addEventListener('mousemove', (e) => {
      cursorRing.style.left = `${e.clientX}px`;
      cursorRing.style.top = `${e.clientY}px`;
      cursorDot.style.left = `${e.clientX}px`;
      cursorDot.style.top = `${e.clientY}px`;
    });
    
    // Make click effect
    document.addEventListener('click', () => {
      cursorRing.style.transform = `translate(-50%, -50%) scale(0.8)`;
      setTimeout(() => {
        cursorRing.style.transform = `translate(-50%, -50%) scale(1)`;
      }, 100);
    });
  });
</script>

<script>
  // Animate number counters while preserving decimals
  function animateCounters() {
    const elements = [
      { id: 'employeeRatio', end: 1.56, isDecimal: true },
      { id: 'totalEmployees', end: 136, isDecimal: false },
      { id: 'serviceCoverage', isStatic: true }, // This won't animate
      { id: 'guestRatio', end: 3, isRatio: true }
    ];

    elements.forEach(item => {
      const element = document.getElementById(item.id);
      if (element) {
        if (item.isStatic) return; // Skip animation for static values
        
        if (item.isRatio) {
          animateRatioValue(element, item.end);
        } else {
          animateDecimalValue(element, item.end, item.isDecimal);
        }
      }
    });
  }
  
  function animateDecimalValue(element, end, keepDecimals) {
    const duration = 1500;
    const start = 0;
    let startTimestamp = null;
    
    const step = (timestamp) => {
      if (!startTimestamp) startTimestamp = timestamp;
      const progress = Math.min((timestamp - startTimestamp) / duration, 1);
      const value = progress * (end - start) + start;
      
      // Preserve decimals exactly as they are
      if (keepDecimals) {
        element.textContent = value.toFixed(2);
      } else {
        element.textContent = Math.round(value).toString();
      }
      
      if (progress < 1) {
        window.requestAnimationFrame(step);
      }
    };
    window.requestAnimationFrame(step);
  }
  
  function animateRatioValue(element, end) {
    const duration = 1500;
    const start = 0;
    let startTimestamp = null;
    
    const step = (timestamp) => {
      if (!startTimestamp) startTimestamp = timestamp;
      const progress = Math.min((timestamp - startTimestamp) / duration, 1);
      const value = Math.round(progress * (end - start) + start);
      element.textContent = `${value}:1`;
      
      if (progress < 1) {
        window.requestAnimationFrame(step);
      }
    };
    window.requestAnimationFrame(step);
  }

  // Initialize when summary becomes visible
  const summaryObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        animateCounters();
      }
    });
  }, { threshold: 0.1 });
  
  const summary = document.querySelector('.employee-summary');
  if (summary) {
    summaryObserver.observe(summary);
  }
</script>

</body>
</html>